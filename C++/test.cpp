#include <iostream>
#include <fstream>
#include <vector>
#include <algorithm>

using namespace std;

int main() {
    int T;
    ifstream inStream("input.txt");
    inStream >> T;
    for (int i = 0; i < T; i++) {

    }
    inStream.close();
}